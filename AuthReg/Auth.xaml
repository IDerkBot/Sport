<Page x:Class="Sport.AuthReg.Auth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d" 
      d:DesignHeight="140" d:DesignWidth="200"
      Title="Auth">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Введите логин:" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" x:Name="login" HorizontalAlignment="Center"/>
        <Label Grid.Row="2" Content="Введите пароль:" HorizontalAlignment="Left"/>
        <TextBox Grid.Row="3" x:Name="password" HorizontalAlignment="Center"/>
        <Button Grid.Row="4" Name="LogIn" Click="LogIn_Click" Content="Войти" HorizontalAlignment="Right" Width="80"/>
        <Button Grid.Row="4" Name="RegIn" Click="RegIn_Click"  Content="Регистрация" HorizontalAlignment="Left" Width="100"/>
    </Grid>
</Page>